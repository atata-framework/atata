namespace Atata
{
    /// <summary>
    /// Specifies the kind of the element selector for the waiting.
    /// </summary>
    public enum WaitBy
    {
        Id,
        Name,
        Class,
        Css,
        XPath
    }
}
