namespace Atata;

/// <summary>
/// Represents an interface for objects that can reset a counter.
/// </summary>
public interface IResetsCounter
{
    /// <summary>
    /// Resets the counter to its initial state.
    /// </summary>
    void ResetCounter();
}
