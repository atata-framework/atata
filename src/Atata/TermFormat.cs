namespace Atata
{
    public enum TermFormat
    {
        Inherit,
        Title,
        Sentence,
        LowerCase,
        UpperCase,
        Camel,
        Pascal,
        Dashed,
        XDashed,
        Underscored
    }
}
