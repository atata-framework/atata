namespace Atata
{
    public enum TermFormat
    {
        Inherit,
        None,
        Title,
        Sentence,
        LowerCase,
        UpperCase,
        Camel,
        Pascal,
        Dashed,
        XDashed,
        Underscored
    }
}
